// The lab drives the same splitter play does, so a comparison here is a comparison of play.
export { settledBoxCount as settledBoxes, splitNarration as buildChunking } from '@/features/play'
export type { NarrationSplit as SynthesisUnit, NarrationUnits as LabChunking } from '@/features/play'
